# Agent Project Instructions Template

## 0. 工作原则

本项目用于维护可迁移的 AI Agent 数字转生包。

核心要求：
- 结论先行（BLUF）。
- 不知道就说不知道。
- 不编造用户经历、业务数据、市场结论或项目规模。
- 长期记忆必须落盘到 Markdown 文件，并可通过 Git 审计。
- checkpoint 时更新本地文件、维护日志，并在 remote（远程仓库地址）可用时 push（推送到云端备份）。

## 1. 文件删除安全协议

禁止批量删除文件或目录。不得使用递归删除、通配符删除、目录删除或跨目录批量清理命令。

禁止使用：
- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`
- 任何带通配符的删除命令

允许的删除模式：
- 仅在用户明确授权后，一次删除一个清晰、完整、非目录的文件路径。
